{
  "name": "@catan/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./platform": "./src/platform/index.ts",
    "./catan": "./src/catan/index.ts",
    "./draw-guess": "./src/draw-guess/index.ts",
    "./transport": "./src/platform-stream.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node ../../scripts/testing/run.mjs unit --layer protocol",
    "test:all": "node ../../scripts/testing/run.mjs unit --scope all --layer protocol"
  },
  "dependencies": {
    "@catan/game-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
